{
  "compilerOptions": {
    "target": "esnext",                               /* The target JavaScript version for the emitted JavaScript. */
    "module": "esnext",                               /* Specify the module code generation. */
    "moduleResolution": "node",                       /* Resolves modules the same way Node.js does. */
    "jsx": "react-jsx",                               /* Enable the new JSX transform for React. */
    "esModuleInterop": true,                          /* Ensures compatibility between CommonJS modules and ES6 modules. */
    "forceConsistentCasingInFileNames": true,          /* Ensures consistent casing in file names. */
    "strict": true,                                   /* Enables all strict type-checking options in TypeScript. */
    "skipLibCheck": true,                             /* Skip type checking of declaration files for better performance. */
    "noUnusedLocals": true,                           /* Reports errors on unused local variables. */
    "noUnusedParameters": true,                       /* Reports errors on unused function parameters. */
    "allowJs": true,                                  /* Allows JavaScript files to be part of the project. */
    "checkJs": true,                                  /* Enables type-checking in JavaScript files. */
    "resolveJsonModule": true,                        /* Allows importing of JSON files. */
    "types": ["react", "react-dom", "vite/client"],                  /* Ensures TypeScript includes React and React DOM types globally. */
    "baseUrl": ".",                                   /* Specifies the base directory for relative module imports. */
    "paths": {
      "@/*": ["src/*"]                                /* Maps the '@' prefix to the 'src' directory. */
    }
  },
  "include": [
    "src"                                             /* Includes all files in the src directory. */
  ],
  "exclude": [
    "node_modules",                                   /* Excludes the node_modules directory. */
    "build",                                          /* Excludes the build directory if you have one. */
    "dist"                                            /* Excludes the dist directory if you have one. */
  ]
}
